# Локальная разработка.
# Запуск из корня репозитория:
#   docker compose -f docker/docker-compose.local.yml up --build
# Для авто-перезапуска при изменении кода:
#   docker compose -f docker/docker-compose.local.yml watch
#
# build-context — корень репозитория (..), а не папка docker/.

name: max-id-bot-local

services:
  bot:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: max-id-bot:local
    env_file:
      - ../.env
    environment:
      LOG_LEVEL: ${LOG_LEVEL:-DEBUG}
      # Внутри контейнера порт фиксирован: наружу его публикует секция ports.
      WEBHOOK_HOST: 0.0.0.0
      WEBHOOK_PORT: 8080
    # Нужно только при BOT_MODE=webhook; в режиме polling порт просто не слушается.
    ports:
      - "8080:8080"
    restart: "no"
    # compose watch: синхронизируем исходники без пересборки,
    # пересобираем образ только при изменении зависимостей
    develop:
      watch:
        - action: sync+restart
          path: ../src
          target: /app/src
          ignore:
            - .venv/
            - "**/__pycache__/"
        - action: rebuild
          path: ../pyproject.toml
        - action: rebuild
          path: ../uv.lock
